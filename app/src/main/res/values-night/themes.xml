<resources>
    <!-- Base application theme. -->
    <style name="Theme.Story"
        parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/main_color</item>
        <item name="colorPrimaryVariant">@color/status_bar_color</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_200</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/status_bar_color</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Story.Button.Primary"
        parent="Widget.MaterialComponents.Button">
        <item name="shapeAppearance">@style/ShapeAppearance.Story.Button.Rounded</item>
        <item name="android:backgroundTint">@color/btn_generic_background_color</item>
        <item name="android:paddingTop">@dimen/btn_story_padding_vertical</item>
        <item name="android:paddingBottom">@dimen/btn_story_padding_vertical</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">@dimen/btn_story_text_size</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="userName">
        <item name="android:textColor">@color/white</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="description">
        <item name="android:textColor">@color/white</item>
    </style>

</resources>